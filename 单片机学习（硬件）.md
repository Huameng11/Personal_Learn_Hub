## 单片机学习（硬件）

### 按键硬件消抖方案

#### 一、消抖核心目的

解决机械按键触点反弹导致的高频 “毛刺信号”，避免单片机误判为多次按键操作（抖动持续时间通常 10-20ms）。

#### 二、四大主流硬件消抖方案（按成本从低到高）

##### 1. RC 积分电路（最常用低成本方案）

- **核心原理**：利用电容 “电压不能突变” 的特性，通过电阻限制充放电速度，过滤高频抖动（充放电时间 0.7×R×C 需大于抖动时间）。
- **关键参数**：典型取值 R=10kΩ、C=1μF（充放电 7ms），抖动严重时可加大 C（如 2μF）。
- **特点**：电路最简单、成本最低，无需额外芯片；缺点是有轻微延迟，电容老化可能影响稳定性。
- **通俗类比**：蓄水池 + 细水管，快速开关水龙头（抖动）时，水位（电平）来不及波动，输出稳定水流（信号）。

##### 2. 施密特触发器方案

- **核心原理**：利用 “滞回特性”（高低电平切换有不同阈值），仅当输入信号超过上限阈值（变高）或低于下限阈值（变低）时才切换输出，过滤中间抖动。
- **关键器件**：施密特反相器（如 74HC14），单门电路即可实现。
- **特点**：响应速度比 RC 电路快，无电容老化问题；缺点是抗干扰能力略弱于 RS 触发器。
- **通俗类比**：双门槛闸门，进门跨 1 米、出门低于 0.5 米，小幅度晃动（抖动）跨不过门槛，不误触发。

##### 3. RS 触发器方案（高稳定性首选）

- **核心结构**：2 个与非门（如 74HC00）交叉反馈组成，含 S（置位）、R（复位）端。
- **核心原理**：双稳态记忆功能，触发后输出电平通过交叉反馈 “锁定”，抖动信号无法打破闭环（与非门逻辑：有 0 出 1，全 1 出 0）。
- **特点**：消抖无延迟、抗干扰极强、无电容依赖；缺点是需额外逻辑门芯片，成本高于 RC 和施密特。
- **通俗类比**：自锁灯开关，按下锁定（亮）、松开锁定（灭），手抖晃动不会改变状态。
- **关键接线**：按键配合上拉 / 下拉电阻，确保 S/R 端未触发时电平稳定，避免悬空误触发。

##### 4. 专用消抖芯片方案（量产标准化方案）

- **核心原理**：芯片内置施密特触发器、RC 模块和信号整形电路，直接输入按键原始信号，输出稳定电平。
- **典型芯片**：MAX6816（单键）、MAX6817（双键）、74HC14（六路施密特，可间接消抖）。
- **特点**：无需手动调参数，消抖效果标准化，兼容性强；缺点是成本最高，DIY 场景性价比低。
- **通俗类比**：现成专业净水器，无需自己搭配滤芯（R/C），插上即用，效果稳定。

