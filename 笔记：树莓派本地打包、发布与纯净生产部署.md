#  笔记：树莓派本地打包、发布与纯净生产部署

本笔记记录了如何将一个 Python 应用程序（如大管家 `familyBot`）在树莓派本地完成镜像容器化打包，并发布到云端，最后实现源码与生产环境彻底分离的规范化流程。

## 🛠️ 准备工作：标准项目目录结构

首先注册一个dockerhub账号，百度搜索官网注册即可。254521335

在打包前，树莓派开发目录（如 `~/familyBot`）需要确保包含以下核心文件：

```Plaintext
~/familyBot/
├── Dockerfile           # 镜像构建蓝图（不带后缀名）
├── docker-compose.yml   # 容器本地编排文件
├── .env                 # 动态环境变量配置文件
├── requirements.txt     # 项目 Python 依赖包清单
├── main.py              # 程序入口
├── config.py / routers/ # 其它源码
└── data/                # 持久化数据文件夹
    └── medicine.db      # 真实的 SQLite 数据库文件
```

## 📐 第一阶段：核心配置文件编写

### 1. 编写通用的 `Dockerfile`

用于规范容器内部的环境，将 Python 源码和依赖打包进一个只读镜像中。



```Dockerfile
# 使用官方通用的轻量级 Python 镜像
FROM python:3.11-slim

# 🚀 核心修复：在构建阶段安装时区数据并设定为上海时区
RUN apt-get update && \
    DEBIAN_FRONTEND=noninteractive apt-get install -y tzdata && \
    ln -fs /usr/share/zoneinfo/Asia/Shanghai /etc/localtime && \
    echo "Asia/Shanghai" > /etc/timezone && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app

# 安装依赖（包括刚刚装的 python-multipart）
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制源码
COPY . .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 2. 编写开发/构建版 `docker-compose.yml`

通过同时配置 `build: .` 和 `image`，让 Docker Compose 既能本地编译，又能自动打好带有用户名的标签，方便一键推送。



```YAML
services:
  familybot:
    # 1. 明确镜像名字（方便后续 docker compose push 发布）
    image: 254521335/familybot:latest
    # 2. 本地构建
    build: .
    container_name: familybot_server
    # 🚀 增加环境变量，明确告诉 Python 和 操作系统使用上海时区
    environment:
      - TZ=Asia/Shanghai
    ports:
      - "8000:8000"
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
      # 🚀 将树莓派宿主机的时间文件只读挂载给容器，保持硬件时间绝对一致
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    restart: always
```

## 🚀 第二阶段：本地编译与发布到 Docker Hub

> 💡 **避坑前置条件**：在打包启动前，必须通过 `sudo systemctl stop familybot.service` 停止原先的系统级自启服务，或通过 `sudo fuser -k 8000/tcp` 清空占用，把 **8000 端口** 释放给 Docker。

### 1. 登录 Docker Hub 账户（只需登录一次）

在树莓派终端输入命令，验证你的云端分发身份：

```Bash
docker login
```

*根据提示输入你的 Docker Hub **用户名（254521335）** 和 **密码**。看到 `Login Succeeded` 说明凭证已成功保存在本地。*

### 2. 本地一键编译与发布组合拳

代码修改完毕，或者依赖更新（在开发环境中运行 `pip freeze > requirements.txt`）后，在 `~/familyBot` 下直接运行：

```Bash
#0.导出依赖
pip freeze > requirements.txt

# 1. 在树莓派本地编译出原生的 ARM64 镜像
docker compose build

# 2. 一键将编译好并带好标签的镜像推送到 Docker Hub 云端
docker compose push
```

## 🏆 第三阶段：无缝迁移至“纯净生产环境”

由于源码和依赖已经安全隔离在云端镜像内部，树莓派的生产环境可以实现**完全去除源码**的极简部署。

### 1. 建立纯净的生产目录并迁移资产

```Bash
# 1. 停止当前正在运行的开发容器
docker compose down

# 2. 新建一个专用于生产运行的文件夹
mkdir -p ~/familybot-prod/data

# 3. 将你的核心资产——持久化数据库和 .env 配置文件安全复制过去
cp ~/familyBot/data/medicine.db ~/familybot-prod/data/
cp ~/familyBot/.env ~/familybot-prod/

# 4. 彻底放开 data 文件夹的读写权，一劳永逸防止 SQLite 闪退
chmod -R 777 ~/familybot-prod/data
```

### 2. 编写极简生产版 `docker-compose.yml`

进入新创建的生产目录 `cd ~/familybot-prod`，编写一个**不需要源码、不含 build 逻辑**的纯净编排文件，加了时区：



```YAML
version: '3.8'

services:
  familybot:
    # 1. 明确镜像名字（方便后续 docker compose push 发布，或者直接拉取）
    image: 254521335/familybot:latest
    # 2. 本地构建（保留此处不影响拉取；如果纯拉取，这行也可以不删，Docker 会优先识别 image）
    build: .
    container_name: familybot_server
    
    # 🚀 无论拉取还是构建，都必须注入的时区环境变量
    environment:
      - TZ=Asia/Shanghai
      
    ports:
      - "8000:8000"
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
      
      # 🚀 无论拉取还是构建，都必须挂载的宿主机时区文件
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      
    restart: always
```

### 3. 一键秒级启动与验证

```Bash
# 在生产线下，一键拉取镜像并常驻后台启动
docker compose up -d

# 查看实时启动日志
docker compose logs -f
```

看到 `Application startup complete.`，代表大管家系统完美上线！

## 🔄 终极版日常开发迭代工作流

这套架构打通后，你未来的全平台迭代开发将变得极其清爽：

1. **在 Windows 或树莓派上改代码**：修改源码测试满意后，在你的开发文件夹下运行 `docker compose build && docker compose push`，将最新的原生 ARM64 镜像推送到云端。

2. **在树莓派生产环境应用更新**：进入 `~/familybot-prod` 目录，无脑敲下：

   ```Bash
   docker compose pull && docker compose up -d
   ```

   树莓派会用几秒钟时间拉取增量镜像层并重启容器。源码完全不可见，生产环境数据（`data/medicine.db`）安稳如初！